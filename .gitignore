# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite dev cache
.vite/

# Private planning documents — the audit, model specs, methodology redraft and
# roadmap live in the working tree while the v2 ("Beta") work is underway, but
# each is marked "not committed to the repository". Keep it that way: a stray
# `git add -A` must never publish them.
METHODOLOGYAUDIT.md
METHODOLOGYPAGE.md
MODELSPECS.md
ZEROMANDERROADMAP.md

# Dev-only anchor-comparison harness (delete at the Beta freeze)
suburb-variants.html
